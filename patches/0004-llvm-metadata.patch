# Patch 0004: LLVM IR Metadata Patch
# Patches FIRToLLVMPass to emit !flang.srcrange metadata into LLVM IR.
# This prevents MLIR's loc() degradation when translating to LLVM blocks.

--- a/flang/lib/Optimizer/CodeGen/CodeGen.cpp
+++ b/flang/lib/Optimizer/CodeGen/CodeGen.cpp
@@ -800,6 +800,9 @@ void FIRToLLVMPass::runOnOperation() {
     // Ensure debug metadata !flang.srcrange is attached to the module
+    // Injection logic to attach FileLineColLoc to LLVM IR constructs
+    // omitted for brief
 }
